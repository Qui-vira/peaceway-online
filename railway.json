{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ \"$PW_ROLE\" = worker ]; then exec uvicorn app.worker_app:app --host 0.0.0.0 --port $PORT; else alembic upgrade head && exec uvicorn app.main:app --host 0.0.0.0 --port $PORT; fi",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
