# Exclude local dev artifacts from Railway uploads
.venv/
venv/
web/node_modules/
web/.next/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.env
.env.*
!.env.example
docs/
tests/
scripts/
# Heavy non-backend assets - the Next.js frontend deploys on Vercel, so the
# Railway Python services never need any of these.
web/
presentations/
graphify-out/
design-system/
node_modules/
.codex/
peaceway-online-pharmacy-website/
*.md
# Untracked local Node tooling at the repo root - if uploaded, NIXPACKS builds
# a Node app (no pip/uvicorn) instead of the Python backend. The web service
# (GitHub source) has no root package.json, so this matches its Python build.
/package.json
/package-lock.json
